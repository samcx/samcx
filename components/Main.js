import css from '../css/main.css';

export default function Main() {
  return (
    <main className={css.main}>
      <section>
        <h1 className={css.title}>Hi! I'm Sam. Full-stack web developer.</h1>
      </section>
    </main>
  );
}
